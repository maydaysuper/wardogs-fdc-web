# 国内直连部署（阿里云 OSS 或 腾讯云 COS）

GitHub Pages 在大陆经常打不开。静态文件放到 OSS/COS 后，朋友用默认域名就能开，**先不用备案**。

两家做一家即可。推荐阿里云上海，控制台更直观。

---

## 方案 A · 阿里云 OSS（推荐）

### 1. 开通

1. 打开 https://oss.console.aliyun.com
2. 地域选 **华东1（上海）** `oss-cn-shanghai`
3. 创建 Bucket
   - 名称：`wardogs-fdc`（全局唯一，被占用就加数字）
   - 读写权限：**公共读**
   - 服务端加密：无
4. Bucket → **数据安全 / 读写权限** 确认是公共读
5. Bucket → **数据管理 → 静态页面**
   - 默认首页：`index.html`
   - 默认 404：`404.html`
   - 打开静态网站托管

### 2. 子账号密钥（不要用主账号）

1. https://ram.console.aliyun.com → 身份管理 → 用户 → 创建用户
2. 勾选 **OpenAPI 调用**
3. 创建后点进去 → **创建 AccessKey**，把 ID / Secret 复制下来（只显示一次）
4. 权限管理 → 添加权限：`AliyunOSSFullAccess`（或只授权这一个 Bucket）

### 3. 填到 GitHub

仓库 → Settings → Secrets and variables → Actions → Secrets，新建：

| 名称 | 值 |
|---|---|
| `ALIYUN_ACCESS_KEY_ID` | 刚才的 AccessKey ID |
| `ALIYUN_ACCESS_KEY_SECRET` | AccessKey Secret |
| `ALIYUN_OSS_BUCKET` | `wardogs-fdc`（你的 Bucket 名） |
| `ALIYUN_OSS_ENDPOINT` | `oss-cn-shanghai.aliyuncs.com` |

### 4. 发布

push `main` 会自动跑 **Deploy China OSS/COS**。也可以 Actions 里手动 Run workflow。

### 5. 给朋友的链接

```
https://wardogs-fdc.oss-cn-shanghai.aliyuncs.com/
```

把 `wardogs-fdc` 换成你的 Bucket 名。用浏览器直接打开，不需要梯子。

---

## 方案 B · 腾讯云 COS

### 1. 开通

1. https://console.cloud.tencent.com/cos
2. 地域 **上海** `ap-shanghai`
3. 创建存储桶，访问权限 **公有读私有写**
4. 存储桶 → 基础配置 → **静态网站**：开启，索引 `index.html`，错误 `404.html`

### 2. 密钥

1. https://console.cloud.tencent.com/cam/capi
2. 新建密钥，复制 SecretId / SecretKey
3. 不要用主账号长期密钥的话，建子用户并授 `QcloudCOSFullAccess`

### 3. GitHub Secrets

| 名称 | 值 |
|---|---|
| `COS_SECRET_ID` | SecretId |
| `COS_SECRET_KEY` | SecretKey |
| `COS_BUCKET` | `wardogs-fdc-1234567890`（控制台显示的桶名，带 APPID） |
| `COS_REGION` | `ap-shanghai` |

链接类似：

```
https://wardogs-fdc-1234567890.cos.ap-shanghai.myqcloud.com/
```

---

## 可选：CDN 加速（仍可不备案）

OSS/COS 默认域名已经能给国内朋友用。流量大了再开：

- 阿里云：CDN → 添加域名，源站填这个 Bucket。绑定自己的域名且选「中国内地」节点时，**域名必须备案**。
- 只用默认 `*.aliyuncs.com` / `*.myqcloud.com`：**不用备案**。

香港 Bucket 也不用备案，但上海/广州对大陆延迟更好。

---

## 构建说明

国内构建 `npm run build:china`：

- 站点根路径 `/`（不是 github.io 子目录）
- 地图只用仓库里的本地瓦片，不请求境外 `wardogs-artillery.com`

GitHub Pages 流程还在，海外链接照旧。
